---
id: "017"
title: "path length"
part: "Part 4. Path sampling"
demo: "017"
---

# path length

flatten 후 segment 길이 합으로 path length를 구합니다.

<LessonDemo id="017" />

## 핵심

- `pathLength`
- dasharray·motion path
- closed path 끝점 포함

## Core API (`svg-matrix-core`)

이 레슨 데모는 `packages/lesson_runtime/lesson-app.js`의 `path-length` 데모와 `svg-matrix-core` 함수를 연결합니다.

## 오늘의 핵심

개념을 코드로 고정하세요. 브라우저 SVG는 결과를 보여 주고, 편집기는 같은 수식을 직접 실행해야 합니다.
