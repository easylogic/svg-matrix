---
id: "084"
title: "evenodd parity → pixels"
part: "Part 20. Tessellation & pixels"
demo: "geom-evenodd-pixel"
---

# evenodd parity → pixels

ray crossing 홀수면 inside — scanline fill과 동일 규칙(025).

<LessonDemo id="084" />

## Core API

`evenoddParityFromRayCast` — `packages/svg-matrix-core/src/geometry.js`

## 오늘의 핵심

수식을 `svg-matrix-core`에서 직접 실행해 보세요. 브라우저 SVG는 결과만 보여 주고, 편집기는 같은 코드가 필요합니다.
