---
id: "019"
title: "linearGradient 좌표"
part: "Part 5. Paint servers"
demo: "019"
---

# linearGradient 좌표

gradient 축 위 projection으로 stop 파라미터 t를 구합니다.

<LessonDemo id="019" />

## 핵심

- `linearGradientStopParameter`
- `objectBoundingBoxToUserSpace`
- Figma gradient handle과 연결

## Core API (`svg-matrix-core`)

이 레슨 데모는 `packages/lesson_runtime/lesson-app.js`의 `gradient-units` 데모와 `svg-matrix-core` 함수를 연결합니다.

## 오늘의 핵심

개념을 코드로 고정하세요. 브라우저 SVG는 결과를 보여 주고, 편집기는 같은 수식을 직접 실행해야 합니다.
