---
id: "010"
title: "S, T — smooth continuation"
part: "Part 1. Path grammar"
demo: "010"
---

# S, T — smooth continuation

이전 segment의 control을 반사해 C/Q를 매끄럽게 이어 붙입니다.

<LessonDemo id="010" />

## 핵심

- S → reflected cp1 + explicit cp2
- T → reflected Q control
- 필기 stroke에 유용

## Core API (`svg-matrix-core`)

이 레슨 데모는 `packages/lesson_runtime/lesson-app.js`의 `path-smooth` 데모와 `svg-matrix-core` 함수를 연결합니다.

## 오늘의 핵심

개념을 코드로 고정하세요. 브라우저 SVG는 결과를 보여 주고, 편집기는 같은 수식을 직접 실행해야 합니다.
