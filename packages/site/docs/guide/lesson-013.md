---
id: "013"
title: "miter length 공식"
part: "Part 2. Stroke geometry"
demo: "013"
---

# miter length 공식

miter spike 길이를 각도·stroke width로 계산하고 miterLimit과 비교합니다.

<LessonDemo id="013" />

## 핵심

- `miterLength(joinAngle, strokeWidth)`
- `shouldBevelJoin`
- 브라우저 stroke-linejoin과 맞추기

## Core API (`svg-matrix-core`)

이 레슨 데모는 `packages/lesson_runtime/lesson-app.js`의 `miter-math` 데모와 `svg-matrix-core` 함수를 연결합니다.

## 오늘의 핵심

개념을 코드로 고정하세요. 브라우저 SVG는 결과를 보여 주고, 편집기는 같은 수식을 직접 실행해야 합니다.
